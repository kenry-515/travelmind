[pytest]
testpaths = tests
addopts = -q --cov=app.agents.dialog_manager --cov=app.agents.itinerary_contract --cov=app.agents.recommendation_agent --cov-report=term-missing
markers =
    external: 需要真实外部 API 的测试（CI 默认排除）
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
