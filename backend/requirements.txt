# TravelMind Agent — All Dependencies (dev + prod)
# Phase 14d: Production deps in requirements-prod.txt
# Install with: pip install -r requirements.txt  (dev)
#               pip install -r requirements-prod.txt  (prod)

-r requirements-prod.txt

# --- Dev / Test ---
pytest==8.3.4
pytest-asyncio==1.4.0
pytest-cov==6.0.0
fakeredis==2.36.2
